# Observability Checklist

Check for:
1. Agent trajectories being saved or logged (e.g., LangSmith, local JSON logs).
2. Per-tool execution duration logs.
3. Token usage metrics tracked per agent run.
4. Eval hooks or `agenteval` / `promptfoo` configurations in the repository.
